const element = (
   <div title="Outer div">
      <h1>Hello World!</h1>
      <h3>I just wanted the watcher to run completely</h3>
   </div>
);
ReactDOM.render(element, document.getElementById('content'));